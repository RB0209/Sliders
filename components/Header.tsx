// components/Header.tsx
import React from "react";

export default function Header() {
  return (
    <header className="absolute top-0 left-0 z-30 p-4">
      {/* Header intentionally left empty — removed "Trust me" and menu */}
    </header>
  );
}
